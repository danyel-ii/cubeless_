{
  "name": "cubeless-miniapp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "next dev --hostname 127.0.0.1 --port 3000",
    "build": "next build",
    "start": "next start",
    "test": "echo \"No frontend tests configured\"",
    "test:ui": "playwright test",
    "coverage:contracts": "cd contracts && python3 scripts/check_coverage.py",
    "check:no-client-secrets": "npm run build && node scripts/check-client-secrets.mjs",
    "fork-test": "bash scripts/fork-test.sh"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1",
    "@types/react": "19.2.7",
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "dependencies": {
    "@farcaster/miniapp-sdk": "^0.2.1",
    "buffer": "^6.0.3",
    "ethers": "^6.16.0",
    "zod": "^3.23.8"
  }
}
